# 如何把新日记融入 Mind OS（无痛更新流程）

这套系统只有一份"真源"：`data/mind-os.json`。网页 `index.html` 是由它自动生成的视图，
**永远不要直接改 `index.html`**——改了也会在下次构建时被覆盖。

## 日常使用（你只需要做这一步）

把新写的日记片段（一段话、一篇 Google Doc、一张手机备忘录截图都行）发给我，说一句类似：

> "这是我这周的新日记，帮我融进思维库。"

我会做的事（你不需要操心细节）：

1. 用 `mind-os-classifier` 技能，把内容拆成独立的认知单元，按你的四象限十二子类分类。
2. 对每条逐一判断：
   - **强化已有条目** → 在对应 principle 的 `cases[]` 里追加一条新案例（带日期），
     如果原文有值得保留的原句，顺手摘进 `quotes[]`。
   - **全新的洞察** → 新增一条 principle（编号在所在子类现有最大编号基础上 +10；
     如果要插在中间，用两者之间的整数，比如 `MAS-A-21`）。
   - **纯生活记录**（旅行、孩子的趣事、日常琐碎）→ 追加进 `lifeRecords[]`，
     不进思维库金字塔，只在"生活记录"Tab 里按时间线出现。
   - **噪音**（无信息量的流水账）→ 不收录。
3. 更新永远是**追加**，不改写、不删除你已有的条目和编号。
4. 跑 `python3 src/build.py` 重新生成 `index.html`。
5. 重新发布 Artifact（同一个链接，直接刷新页面就是最新内容），并提交推送到 git 分支。

全程你不需要碰 JSON 或代码，说一句话、等我处理完即可。

## 主题簇（subcat 内的二级分组）

每个子类现在有 2–4 个"主题簇"（`quadrants[].subcats[].clusters`），每条 principle 用 `cluster`
字段（如 `"c1"`）标注属于哪个簇，纯展示层分组，不影响 `id` 的分类含义。新增 principle 时要
判断它属于该子类哪个簇；如果没有合适的簇，可以和用户商量新增一个簇。

## 原始日记片段（rawExcerpt / context）

`quotes[]` 和 `cases[]` 里的每一条可以选配一个 `context` 字段：比现在展示的精炼摘要更长、更接近
当时原始日记的一段文字，用户点"查看原始日记片段"才会展开，用来彻底替代翻旧笔记的需求。
没有 `context` 字段时按钮不会渲染（不会有空按钮）。这一层内容需要回到原始 Drive 日记 PDF 里挖，
是一个持续进行的背景任务，不是每次加新日记都必须补全。

## 「重近轻远」的默认规则

- 2022 年以后的内容默认标 `recency: "recent"`；混合新旧案例的条目标 `"mixed"`。
- 十年前的旧内容默认不收录，除非满足以下条件之一：
  - **佐证十年如一日的一致性**：作为 `root` 案例挂在已有的近年条目下（默认折叠，
    证明"这个想法十年前和今天一样"）。
  - **至今仍适用、但近年日记忘了再提的重大教训**：标 `recency: "evergreen"`，
    即使打开"只看近 3–4 年"开关也会保留可见（这是你明确要求的例外）。
  - 其余旧内容一律不收录，或降级进生活记录。

## 本地开发命令

```bash
# 修改 data/mind-os.json 后，重新生成网页：
python3 src/build.py

# 本地打开检查（可选，需要装 playwright，一般不需要）：
open index.html   # 或直接双击文件在浏览器打开
```

## 关于 src/merge_*.py

`src/merge_2022_2023_root.py`、`src/merge_2024.py`、`src/merge_2025.py` 是这次重建时用来把
十年原始日记的提取结果（原话、新洞察、生活记录）一次性合并进 `data/mind-os.json` 的**一次性迁移脚本**，
已经跑过、完成了使命。以后新增内容不需要再写类似脚本——直接告诉我新日记内容，
按上面"日常使用"的流程走（我会直接编辑 JSON）即可。保留这几个脚本只是为了留一份可追溯的合并记录。

## 文件结构

```
data/mind-os.json      ← 唯一真源：四象限骨架 + 全部 principles + lifeRecords + healthLog
src/template.html      ← 页面模板（HTML/CSS/JS，__MIND_OS_DATA__ 是数据占位符）
src/build.py           ← 构建脚本：把 JSON 内联进模板 → 生成 index.html
index.html             ← 生成产物，自包含单文件，可直接发布为 Artifact 或用浏览器打开
```

## 网页里的"编辑/高亮/标星"是怎么持久化的

网页右上角有个「🖊 编辑」开关。打开后：

- 点进任意文字（关键词/标题/深度解析/行动指南/原话/案例/生活记录）直接改，失焦即保存。
- 选中文字后，底部工具栏可以高亮或改文字颜色。
- 每张卡片左上角的 ☆ 可以一键标星（标记"以后要重点回顾"的点），配合顶部「只看重点」筛选。
- 同一句话如果在「思维库」和「原话集」都出现（比如标题），只需要改一处，因为两边读的是同一份数据。

**重要限制**：这次发布时，Claude 账号还没有开通"云端存储"这个 Artifact 能力，所以这些编辑
**只保存在你当前这台设备/浏览器的本地存储（localStorage）里**，不会自动同步到其它设备，也不会
自动写回 `data/mind-os.json`。编辑工具栏里有三个按钮应对这个限制：

- **导出编辑**：把你所有的本地修改打包成一个小 JSON 文件下载下来。
- **导入编辑**：把之前导出的 JSON 文件读回来（比如换了台设备，或清了浏览器缓存）。
- **清空本机编辑**：误操作多了想重来时用，会二次确认。

如果你想让某次编辑**永久生效**（写进 canonical 的 `data/mind-os.json`，所有人访问都看得到)，
导出那个 JSON 文件发给我，说一句"把这些编辑合并进去"，我会帮你合并、重新构建、重新发布。
以后如果这个账号开通了云端存储能力，我可以把这层 localStorage 换成真正的跨设备自动同步。

## 关于"shadcn/ui 风格"

shadcn/ui 本身是一套 React + Tailwind + Radix 的组件源码，需要打包工具链才能跑。Artifact 是
自包含单文件、且 CSP 禁止拉取任何外部 CDN/脚本，所以没办法真的"安装"这个仓库进来。
这次改版是**手工用原生 CSS/JS 复刻了它的视觉与交互语法**（统一圆角/边框/阴影 token、
Tabs 分段控件、Badge 徽章、卡片内 Accordion 展开、聚焦环等），套在原有的配色系统上——
没有换成 shadcn 默认的灰阶配色，因为那只是套壳，不是你要的视觉方向。
